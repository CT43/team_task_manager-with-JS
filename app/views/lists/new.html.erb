<%= form_for(@list) do |f| %>
  <% if @list.errors.any? %>
    <div id="error_explanation">
      <h2>There were some errors:</h2>

      <ul>
        <% @list.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
<% end %>

<h2>Create a list!</h2>

<%= form_for(@list) do |f| %>

  <%= hidden_field_tag :new_user_num, "#{@user_id}" %>

  <%= f.label :name, "List Name" %>
  <%= f.text_field :name %> <br><br>

  <%= f.fields_for :tasks, @list.tasks.build do |tasks| %>
    <%= tasks.text_field :name %>
  <% end %> <br>
  <%= f.fields_for :tasks, @list.tasks.build do |tasks| %>
    <%= tasks.text_field :name %>
  <% end %> <br>
  <%= f.fields_for :tasks, @list.tasks.build do |tasks| %>
    <%= tasks.text_field :name %>
  <% end %> <br>
  <%= f.fields_for :tasks, @list.tasks.build do |tasks| %>
    <%= tasks.text_field :name %>
  <% end %> <br>
  <%= f.fields_for :tasks, @list.tasks.build do |tasks| %>
    <%= tasks.text_field :name %>
  <% end %> <br>
  <%= f.fields_for :tasks, @list.tasks.build do |tasks| %>
    <%= tasks.text_field :name %>
  <% end %> <br>
  <%= f.fields_for :tasks, @list.tasks.build do |tasks| %>
    <%= tasks.text_field :name %>
  <% end %> <br>
  <%= f.fields_for :tasks, @list.tasks.build do |tasks| %>
    <%= tasks.text_field :name %>
  <% end %> <br>

  <%= f.submit %>
<% end %>
